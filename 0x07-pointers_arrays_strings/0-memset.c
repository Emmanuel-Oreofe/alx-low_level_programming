#include "main.h"
/**
 * _memcpy - copy memory area
 * @dest: char
 * @src: char
 * @n: int
 * Return: char pointer
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int i;

	for ( i = 0; i < n; i++)
		dest[i] = src[i];

	return (dest);
}
